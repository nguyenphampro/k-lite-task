extends ../_layout/layout

block variables
	- var title = 'Setup'
	- var description = 'Description for home page'
	- var bodyclass = 'documents'
	- var selected = 'Setup';
	- var href = '';

block body
	section.home
		.container

			.row
				.col.p-4
					h1.mt-6#sec1 Cài đặt
					hr
					p Trước khi sử dụng bộ kit này bạn phải hiểu rằng đây là bộ mã nguồn dùng để tham khảo, chúng tôi khước từ mọi trách nhiệm khi bạn gặp phải rắc rối về pháp lý hay sử dụng nó để thương mại hoá nó. Bằng cách sử dụng và tuân thủ luật nguồn mở nghĩa là bạn đã chấp nhận sự rủi ro khi sử dụng bộ kit này. Tuy nhiên chúng tôi luôn hỗ trợ bạn fix lỗi trong khả năng và thời gian cho phép.
					p Để cài đặt bộ kit này trước tiên bạn clone source về từ:
					blockquote.blockquote.text-primary git clone https://github.com/baonguyenyam/k-lite-task.git
					p Sau đó tiến hành cài đặt bằng cách nhấp vào file
					blockquote.blockquote.text-primary install.bat
					p Nếu bạn sử dụng Mac thì có thể chạy file
						| 
						code install.sh
					hr
					h5.text-danger 1) config.yml
					p Vào thư mục
						| 
						code k-task
						| 
						| mở file
						code config.yml
						| 
						| để cấu hình các thông số cần thiết trong mục SEO
					p Tại đây bạn sẽ thấy phần
						| 
						code ACTIVE_CONCAT: TRUE
					p a) Nếu giá trị là TRUE nghĩa là khi build sẽ không nén
						| 
						code js/css
						| 
						| trong thư mục pages mà sẽ được load dynamic theo từng page. Khi đó:
					p b) Trong file
						| 
						code src/styles/main.sass
						| 
						| bạn phải xoá đi phần các file sass được import từ thư mục pages
					p Trong file này cũng cho phép bạn gọi các thư viện từ
						| 
						code bower_components
						| 
						| vào bằng cách cấu hình mục concat => js và concat => css

					h5.text-danger 2) config.js
					p Vào thư mục
						| 
						code src/scripts
						| 
						| để cấu hình các tính năng cho hệ thống, phần này có thể xem chi tiết tại 
						a(href="api/documents.html") đây
					h5.text-danger 3) main.sass
					p Bộ kit này chỉ sử dụng viết SASS mà không viết SCSS vì vậy bạn phải tập làm quen nếu chưa dùng. Vào thư mục
						| 
						code src/styles
						| 
						| mở file
						| 
						code main.sass
						| 
						| trong file này bạn có thể import các file mình viết trogn thư mục pages vào
					h5.text-danger 4) PUG
					p Bộ kit này cũng không viết HTML mà sử dụng template PUG làm nền tảng vì vậy bạn phải tập làm quen nếu chưa dùng. Mọi cấu hình được đặt trong file
						| 
						code tempaltes/_layout/_config_menu.pug

					hr
					h1.mt-6#sec1 Build
					p Để chạy Dev bạn chạy lệnh
					blockquote.blockquote.text-primary npm start
					p Để chạy Build deploy lên server bạn chạy lệnh
					blockquote.blockquote.text-primary npm run pro
					p Để chạy Build test offline bạn chạy lệnh
					blockquote.blockquote.text-primary npm run prod
					p Để chạy Build KHÔNG NÉN deploy lên server bạn chạy lệnh
					blockquote.blockquote.text-primary npm run pro-no
					p Để chạy Build KHÔNG NÉN test offline bạn chạy lệnh
					blockquote.blockquote.text-primary npm run prod-no
					p Nếu bạn sử dụng mac hay Ubuntu bạn có thể gọi trực tiếp file app.sh
					blockquote.blockquote.text-primary sh app.sh
					p.text-italic Chúc bạn thành công!

