block variables
if process.argv.slice(3) == '--production'
	//- include _watermark

//- Call Data from folder data
- var _data = site.data
- var _dataJS = site.dataJS
//- Variable site
- var prefix = "viensoi-"
